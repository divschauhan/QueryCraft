rasa==3.6.2
rasa-sdk==3.6.0
python-dotenv>=0.19.0
numpy>=1.22.0
scikit-learn>=1.0.0
tensorflow>=2.8.0

# Windows-compatible dependencies
# Avoid spaCy which requires blis that has build issues on Windows
# Use pre-built wheels when possible
setuptools>=65.5.1
wheel>=0.38.0
# For Windows, use a specific version of spaCy with pre-built wheels
spacy>=3.4.0,<3.5.0
# Explicitly exclude problematic packages
# blis is excluded as it causes build errors on Windows
